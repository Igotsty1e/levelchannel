name: Public Surface Check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  public-surface:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run public-surface guardrail
        run: bash scripts/public-surface-check.sh
